import React from "react"
import { Icon, LoadingWrapper } from "./loading.styles"

const Loading = () => {
  return (
    <LoadingWrapper>
      <Icon />
    </LoadingWrapper>
  )
}

export default Loading
